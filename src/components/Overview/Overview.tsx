

// export const Overview = (text, limit) => {
//   if (text.length <= limit) return text;
//   const spaceIndex = text.indexOf(' ', limit);
//   text = text.slice(0, spaceIndex);
//   return text + ' ...';
// };

// cutDescription(overview, 170)
